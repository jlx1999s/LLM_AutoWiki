# 开发问题汇总

## 问题 01：上传《儿童肺炎支原体肺炎诊疗指南（2025年版）》后，Wiki 变成多个 `Other`，且没有实体关系

### 1. 现象描述

- 用户上传文档后，知识树中没有出现预期的 `Entities` / `Concepts` 页面。
- 知识树出现了 3 个 `Other` 文档。
- 图谱里关系很少或几乎没有，无法体现“实体-概念-来源”的结构化知识网络。

### 2. 现场证据（本次排查）

- 生成文件落在了 `wiki/pediatric-mpp/`，而不是 `wiki/entities/`、`wiki/concepts/`、`wiki/sources/`。
- 该目录下页面（如 `overview.md`、`definitions.md`、`diagnosis.md`、`clinical-classification.md`）没有 YAML frontmatter（文件开头直接是 `# 标题`）。
- `wiki/entities`、`wiki/concepts`、`wiki/sources` 目录为空。

### 3. 用户为什么会看到“3 个 Other”

知识树的分类逻辑是：

1. 优先读取 frontmatter 的 `type`。
2. 若没有 `type`，按路径推断（`/entities/` => `entity`，`/concepts/` => `concept`，`/sources/` => `source`）。
3. 若都不满足，则归类为 `other`。
4. 特例：文件名是 `overview.md` 时，会被归类为 `overview`。

因此本次 4 个页面中：

- `overview.md` 被归类为 `overview`
- `definitions.md`、`diagnosis.md`、`clinical-classification.md` 被归类为 `other`

最终表现为“3 个 Other”。

### 4. 为什么“没有实体关系”

本质是“节点缺失 + 链接不可解析”叠加：

- 没有生成实体/概念页面：`entities`、`concepts` 为空，图谱天然缺关键节点。
- 图谱主要识别 `[[wikilink]]`，不识别普通 Markdown 链接 `[text](./x.md)`。
- 页面中存在 `[[./xxx.md|...]]` 这种相对路径 wikilink；图谱 target 解析未统一去掉 `./` 和 `.md`，会导致目标节点匹配失败，边被丢弃。

### 5. 深层根因分析（架构层）

这个问题不是单点 bug，而是“多层防线缺失”的组合问题：

#### 根因 A：生成阶段是“软约束”，不是“硬约束”

- Prompt 已明确要求输出到 `wiki/entities`、`wiki/concepts` 并带 frontmatter。
- 但 LLM 本质是概率生成，尤其在医学长文场景会偏向“按主题拆章节”而非“按 schema 拆实体/概念”。
- 当模型偏离时，系统没有强制纠偏机制。

一句话：**我们相信了模型会遵守规范，但没有做机器级别的结构校验。**

#### 根因 B：写入阶段只做了“安全校验”，没做“语义校验”

当前写入逻辑只校验：

- 路径必须以 `wiki/` 开头
- 不能越级路径（`../`）

但缺少以下关键校验：

- 是否存在 frontmatter
- 是否存在合法 `type`
- 是否有最小实体/概念产出
- 是否产生可解析关系链接

一句话：**文件能写进去 ≠ 写得符合知识架构。**

#### 根因 C：展示/图谱层对“非标准输入”容错不足

- 知识树会把不规范文件归到 `other`，但没有提示“为什么变成 other”。
- 图谱解析对链接形式比较严格，`./x.md`、Markdown link 等兼容不足。

一句话：**上游不规范 + 下游弱容错，导致用户直观看到“变味”。**

### 6. 这是偶发还是必发？

属于“高概率复发”问题，尤其在以下场景：

- 指南类/标准类文档（天然按章节结构组织，模型更容易输出主题目录而非实体目录）。
- 单文档导入且项目 schema 边界不强时。
- 模型响应质量波动或上下文过长导致约束失真时。

### 7. 影响评估

- 产品层：用户认为“系统不会自动抽实体关系”，信任下降。
- 工程层：`other` 积累后，检索和图谱质量持续下降。
- 运营层：做公开演示时，结果不稳定，影响开源项目口碑。

### 8. 正确的治理思路（不是靠人工补）

必须从“后验修补”升级为“流水线强约束”：

#### 8.1 生成后结构化校验（必做）

对每个生成文件做自动检查：

- 无 frontmatter -> 自动补齐模板
- 无 `type` -> 由目录/内容规则推断并回填
- 非法目录（如 `wiki/pediatric-mpp/*`）-> 自动路由到 `entities/concepts/sources` 或归档到 `wiki/notes` 并标记
- 若本轮无 `entity` / `concept` -> 自动生成最小 stub 页（至少保证图谱节点）

#### 8.2 链接标准化（必做）

统一改写以下链接：

- `[[./a.md|A]]` -> `[[a|A]]` 或 `[[concepts/a|A]]`
- `[A](./a.md)` -> `[[a|A]]`

保证图谱引擎可解析，避免“明明有链接却没有边”。

#### 8.3 质量闸门（必做）

每次 ingest 完成后做质量评分，不达标则提示“结构化失败并可一键修复”：

- `type_coverage`：有 type 的页面占比
- `entity_count` / `concept_count`
- `link_resolve_rate`：链接可解析率
- `orphan_count`：孤立页面数

### 9. 给视频分享可直接用的一段总结

> 这次问题看起来像“模型表现不好”，但本质上是工程化问题：我们只约束了安全，没有约束结构质量。  
> LLM 会偶尔跑偏，这是常态；优秀的系统不是要求模型永远完美，而是把“校验、纠偏、标准化”做成默认流程。  
> 所以这次我们得到的关键结论是：**LLM Wiki 的稳定性来自 pipeline 设计，而不是单次模型输出质量。**

### 10. 后续实施优先级（建议）

1. P0：生成后 frontmatter/type/目录校验与自动修复  
2. P0：链接标准化（wikilink 统一）  
3. P1：图谱解析兼容 Markdown link + 相对路径  
4. P1：Ingest 质量闸门与 UI 可视化提示  
5. P2：按文档类型（指南/论文/网页）使用不同提取模板

---

## 问题 01 对应解决方案（已实现，2026-04-15）

本轮已把“事后人工修补”升级为“写入即治理”，核心是把结构约束放到代码侧，而不是只靠 Prompt。

### A. Ingest 写入层强约束（已落地）

- 在 `autoIngest` 与聊天写入 `executeIngestWrites` 两条链路统一接入“生成纠偏”：
  - 路径重路由：把非规范路径（如 `wiki/pediatric-mpp/*`）自动路由到结构目录（`wiki/entities|concepts|sources|...`）。
  - frontmatter 补全：自动补齐 `type/title/created/updated/tags/related/sources`，并保证 `sources` 必含原始文件名。
  - 标题兜底：无标题时从一级标题或文件名推断，避免空白页和无标题页。
- 效果：即使模型输出偏离 schema，落盘文件也会被强制拉回可治理结构。

### B. 链接标准化（已落地）

- 写入前统一规范链接格式：
  - `[[./a.md|A]]` -> `[[a|A]]`（去掉 `./`、`.md`、`wiki/` 前缀）。
  - `[A](./a.md)` -> `[[a|A]]`（将相对 Markdown 链接转换为 wikilink）。
- 效果：减少“有链接但图谱不认”的隐性损耗，降低 broken-link 与孤儿页的误报。

### C. 图谱与 Lint 解析兼容增强（已落地）

- 图谱 `wiki-graph` 现在同时解析：
  - `[[wikilink]]`
  - Markdown 相对链接 `[text](./x.md)`
- Lint 结构检查同样支持以上两种链接，并在匹配前统一规范 target。
- 效果：历史内容和非标准内容的关系边可被识别，关系网络恢复更稳定。

### D. 历史数据容错（已落地）

- 知识树 `parsePageInfo` 为“无 frontmatter 旧页面”增加语义归类兜底（source/entity/concept 关键字推断）。
- 效果：旧数据不会大量堆到 `Other`，用户侧可读性提升。

### E. 对标结论与本项目实现映射

- `llm_wiki` 风格：Prompt + lint 事后治理。
- `sage-wiki` 风格：流水线约束更强，代码侧质量闸门更前置。
- 我们本次实现：把治理前移到“写入环节”，并补齐解析容错，属于向 `sage-wiki` 风格靠拢。

### F. 仍需继续（下一步）

- 增加 ingest 质量闸门指标与 UI（`type_coverage/link_resolve_rate/orphan_count`）。
- 当“实体/概念产出为 0”时自动触发一次轻量二次提取（仅抽实体概念，不重写全部页面）。
- 增加 E2E 回归样例（医疗指南、标准规范、网页长文）保证发布稳定性。

### G. 2026-04-16 追加修复（已落地）

- **空源拦截**：ingest 读取源文件时，若正文字符数过低会直接失败，不再继续让 LLM“猜内容”生成。
  - 同时增加 `.cache/{filename}.txt` 回退读取，降低偶发读空风险。
- **质量闸门**：生成完成后增加自动判定，出现以下情况会直接判失败：
  - stub 页面占比异常偏高；
  - 主要页面中大量出现“fenced frontmatter 模板块”（` ```yaml + --- `）；
  - 主页面过少但补洞 stub 过多。
- **失败快停**：队列将上述错误视为不可重试错误，避免同一错误连续重试 3 次污染结果。

> 这次追加修复的目标是：把“空输入导致的伪结构产出”在流水线中直接拦截，而不是让错误结果落盘后再人工清理。

---

## 复盘方法（可复用）

后续每次“生成结果变味”都按这 4 步排查：

1. 看文件落点：是否进了正确目录（entities/concepts/sources）  
2. 看 frontmatter：是否有合法 `type/title/sources`  
3. 看链接形态：是否统一为可解析 wikilink  
4. 看图谱输入：节点是否存在、边是否可解析

这个复盘模板可以作为项目的长期工程规范。
